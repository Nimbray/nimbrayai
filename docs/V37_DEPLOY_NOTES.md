# Déploiement V37

V37 peut remplacer V36 directement dans le repo Vercel.

```powershell
git add .
git commit -m "NimbrayAI V37 emotional variation engine"
git push
```

Vercel redéploiera automatiquement.

Priorité de test après déploiement : vérifier que les phrases émotionnelles ne déclenchent pas Groq et ne répètent pas toutes la même réponse.
