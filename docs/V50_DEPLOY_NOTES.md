# V50 — Deploy Notes

```powershell
git add .
git commit -m "NimbrayAI V50 new era public beta"
git push
```

Après déploiement, attendre 5 à 10 minutes, tester mobile, tester les phrases sensibles, vérifier que les erreurs Groq ne s’affichent jamais en JSON.
